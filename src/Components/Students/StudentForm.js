import React from 'react';
import { Input, Pagination, Table, Avatar, Button, Modal } from 'antd';
import useStyles from '../Common/styles'

const StudentForm = () => {
  return (
    <div style={{
      height: '100%', width: '100%',
      display: 'grid',
      gridTemplateColumns: 'minmax(1fr, 1fr)',// almost equals to '50% 50%',
      gridTemplateRows: '40px 40px 40px 40px 40px 40px', // almost equal to '100%',
      gridGap: '15px'
    }}>

    </div>
  )
}
export default StudentForm;